console.log("hello world!")

var years = 1
var days = 365 * years
var hours = 24 * days
var minutes = 60 * hours

console.log("There are " + minutes + " in a year")

// note, these are equivalent:

let example_function = function(a, b) {
    return a + b
}

let example_function2 = (a, b) => {

}